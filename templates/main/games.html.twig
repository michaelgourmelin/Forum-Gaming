{% extends 'base_game.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-4">
                <img src="{{ game.background_image }}" alt="{{ game.name }}" class="img-fluid">
            </div>
            <div class="col-md-6">
                <h1>{{ game.name }}</h1>
                <p><strong>Date de sortie :</strong> {{ game.released }}</p>
                <p><strong>Évaluation Metacritic :</strong> {{ game.metacritic }}</p>
                <p><strong>Description :</strong></p>
                <p>{{ game.description | raw }}</p>
                <p><strong>Site Web :</strong> <a href="{{ game.website }}" target="_blank">{{ game.website }}</a></p>
            </div>
        </div>
    </div>
{% endblock %}
